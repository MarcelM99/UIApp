import { StyleSheet } from "react-native";

const styles=StyleSheet.create({
    backgroundImage:{
        height: '20%',
        alignItems: 'center',
        marginTop:40,
        marginHorizontal: 80,
        marginBottom:20
      },
      subTitle:{textAlign: 'center', fontSize: 10, marginTop: 16}

})

export default styles;